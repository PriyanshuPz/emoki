import { Joti_One, Merienda, Satisfy } from "next/font/google";

export const gameFont = Joti_One({
  variable: "--font-default",
  subsets: ["latin"],
  weight: "400",
});

export const chitFont = Merienda({
  variable: "--font-chit",
  subsets: ["latin"],
  weight: "400",
});
